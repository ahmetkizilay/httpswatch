{
  "name": "httpswatch",
  "description": "Triggers AWS Lambda function to create daily reports of HTTPS availability",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "aws-sdk": "^2.7.20",
    "promise": "^7.1.1"
  },
  "scripts": {
    "clean": "rm -rf ./build",
    "build": "bash ./deploy-utils/build.sh && bash ./deploy-utils/update-template-version.sh",
    "upload": "bash ./deploy-utils/upload-to-s3.sh",
    "update": "bash ./deploy-utils/update-stack.sh httpswatch",
    "clean-build": "npm run clean && npm run build",
    "clean-build-update": "npm run clean-build && npm run update",
    "clean-build-upload-update": "npm run clean-build-update && npm run update"
  }
}
